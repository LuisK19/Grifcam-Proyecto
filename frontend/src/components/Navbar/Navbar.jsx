// Barra de navegación global (logo + enlaces al catálogo e info)

import styles from './Navbar.module.css'

function Navbar() {
  return <nav className={styles.navbar}>Navbar (pendiente)</nav>
}

export default Navbar