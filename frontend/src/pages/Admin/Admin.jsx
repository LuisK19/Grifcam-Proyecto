// Panel administrativo — lista de productos con CRUD

import styles from './Admin.module.css'

function Admin() {
  return <div className={styles.admin}>Panel Admin (pendiente)</div>
}

export default Admin